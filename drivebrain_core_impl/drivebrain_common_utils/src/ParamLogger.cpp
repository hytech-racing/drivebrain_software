#include "ParamLogger.hpp"

namespace util
{
    std::optional<std::string> get_schema(std::vector<core::common::Configurable *> configurable_components)
    {
        
        for(auto component : configurable_components)
        {
            
        }
    }
}