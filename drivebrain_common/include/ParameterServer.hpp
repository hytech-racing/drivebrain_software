#pragma once

// TODO
// - [ ] receive from UDP a protobuf message that gets converted to JSON and ensures that the json schema matches
//       the schema of the internal boost ptree.
// - [ ] be configurable 